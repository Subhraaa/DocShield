spring:
  application:
    name: docshield

  datasource:
    url: jdbc:mysql://localhost:3306/doc_shield
    username: root
    password: Welcome@2024
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect